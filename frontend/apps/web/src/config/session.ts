/**
 * Default session configuration
 * This is a pre-created meeting session that all users will join
 * Using a fixed UUID format
 */
export const DEFAULT_SESSION_ID = '00000000-0000-0000-0000-000000000001';

/**
 * Default session configuration
 */
export const DEFAULT_SESSION_CONFIG = {
  sessionId: DEFAULT_SESSION_ID,
  roomName: 'default-meeting-room',
  description: 'Default meeting room for video calls',
};

